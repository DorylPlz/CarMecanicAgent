# Google API Key (Required)
GOOGLE_API_KEY=your_api_key_here

# LLM Model Configuration (Optional, defaults to gemini-2.5-flash)
LLM_MODEL=gemini-2.5-flash

# Vehicle Configuration (Required)
VEHICLE_MODEL=Subaru WRX
VEHICLE_YEAR=2018
VEHICLE_VIN=your_vin_here

# Optional: Manual PDF path (defaults to service_manual.pdf)
VEHICLE_MANUAL_PDF_PATH=service_manual.pdf

# Optional: Vector Store path (defaults to vector_store)
VECTOR_STORE_PATH=vector_store

# Optional: Aftermarket modifications
VEHICLE_AFTERMARKET_MODS=Cold air Intake Injen,GoFastBits BHV,Custom 3 inch catless downpipe,Stage 2 ECU Custom Tune

# Optional: Indexing Configuration
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200
# EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# Optional: Search Configuration
# TOP_K_RESULTS=10
# SIMILARITY_THRESHOLD=0.5